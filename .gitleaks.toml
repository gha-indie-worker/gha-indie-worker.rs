title = "gha-indie-worker secret rules"

[extend]
useDefault = true

[allowlist]
description = "Cargo.lock contains registry package names and immutable crates.io checksums, not runtime credential values."
paths = [
  '''(^|/)Cargo\.lock$''',
]
