[package]
name = "daedalus-r2-sdk-surface-probe"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
aws-config = { version = "1.10.0", features = ["behavior-version-latest"] }
aws-sdk-s3 = "1.140.0"
tokio = { version = "1", features = ["fs", "macros", "rt-multi-thread"] }
